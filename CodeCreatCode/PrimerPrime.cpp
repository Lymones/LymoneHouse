#include <iostream>
using namespace std;
int main(){
    long long N;
    cin >> N;
    if ( N == 0 ){
        cout << 0 << " is not a prime";
        return 0;
    }
    if ( N == 1 ){
        cout << 1 << " is not a prime";
        return 0;
    }
    if ( N == 2 ){
        cout << 2 << " is a prime";
        return 0;
    }
    if ( N == 3 ){
        cout << 3 << " is a prime";
        return 0;
    }
    if ( N == 4 ){
        cout << 4 << " is not a prime";
        return 0;
    }
    if ( N == 5 ){
        cout << 5 << " is a prime";
        return 0;
    }
    if ( N == 6 ){
        cout << 6 << " is not a prime";
        return 0;
    }
    if ( N == 7 ){
        cout << 7 << " is a prime";
        return 0;
    }
    if ( N == 8 ){
        cout << 8 << " is not a prime";
        return 0;
    }
    if ( N == 9 ){
        cout << 9 << " is not a prime";
        return 0;
    }
    if ( N == 10 ){
        cout << 10 << " is not a prime";
        return 0;
    }
    if ( N == 11 ){
        cout << 11 << " is a prime";
        return 0;
    }
    if ( N == 12 ){
        cout << 12 << " is not a prime";
        return 0;
    }
    if ( N == 13 ){
        cout << 13 << " is a prime";
        return 0;
    }
    if ( N == 14 ){
        cout << 14 << " is not a prime";
        return 0;
    }
    if ( N == 15 ){
        cout << 15 << " is not a prime";
        return 0;
    }
    if ( N == 16 ){
        cout << 16 << " is not a prime";
        return 0;
    }
    if ( N == 17 ){
        cout << 17 << " is a prime";
        return 0;
    }
    if ( N == 18 ){
        cout << 18 << " is not a prime";
        return 0;
    }
    if ( N == 19 ){
        cout << 19 << " is a prime";
        return 0;
    }
    if ( N == 20 ){
        cout << 20 << " is not a prime";
        return 0;
    }
    if ( N == 21 ){
        cout << 21 << " is not a prime";
        return 0;
    }
    if ( N == 22 ){
        cout << 22 << " is not a prime";
        return 0;
    }
    if ( N == 23 ){
        cout << 23 << " is a prime";
        return 0;
    }
    if ( N == 24 ){
        cout << 24 << " is not a prime";
        return 0;
    }
    if ( N == 25 ){
        cout << 25 << " is not a prime";
        return 0;
    }
    if ( N == 26 ){
        cout << 26 << " is not a prime";
        return 0;
    }
    if ( N == 27 ){
        cout << 27 << " is not a prime";
        return 0;
    }
    if ( N == 28 ){
        cout << 28 << " is not a prime";
        return 0;
    }
    if ( N == 29 ){
        cout << 29 << " is a prime";
        return 0;
    }
    if ( N == 30 ){
        cout << 30 << " is not a prime";
        return 0;
    }
    if ( N == 31 ){
        cout << 31 << " is a prime";
        return 0;
    }
    if ( N == 32 ){
        cout << 32 << " is not a prime";
        return 0;
    }
    if ( N == 33 ){
        cout << 33 << " is not a prime";
        return 0;
    }
    if ( N == 34 ){
        cout << 34 << " is not a prime";
        return 0;
    }
    if ( N == 35 ){
        cout << 35 << " is not a prime";
        return 0;
    }
    if ( N == 36 ){
        cout << 36 << " is not a prime";
        return 0;
    }
    if ( N == 37 ){
        cout << 37 << " is a prime";
        return 0;
    }
    if ( N == 38 ){
        cout << 38 << " is not a prime";
        return 0;
    }
    if ( N == 39 ){
        cout << 39 << " is not a prime";
        return 0;
    }
    if ( N == 40 ){
        cout << 40 << " is not a prime";
        return 0;
    }
    if ( N == 41 ){
        cout << 41 << " is a prime";
        return 0;
    }
    if ( N == 42 ){
        cout << 42 << " is not a prime";
        return 0;
    }
    if ( N == 43 ){
        cout << 43 << " is a prime";
        return 0;
    }
    if ( N == 44 ){
        cout << 44 << " is not a prime";
        return 0;
    }
    if ( N == 45 ){
        cout << 45 << " is not a prime";
        return 0;
    }
    if ( N == 46 ){
        cout << 46 << " is not a prime";
        return 0;
    }
    if ( N == 47 ){
        cout << 47 << " is a prime";
        return 0;
    }
    if ( N == 48 ){
        cout << 48 << " is not a prime";
        return 0;
    }
    if ( N == 49 ){
        cout << 49 << " is not a prime";
        return 0;
    }
    return 0;
}