#include <bits/stdc++.h>
#pragma GCC optimize(2)
#define endl "\n"
#define ll long long
#define mm(a) memset(a,0,sizeof(a))
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    char ch[1010];
    cin>>ch;
    vector<int> num;
    vector<char> op;
    int len=strlen(ch);
    for(int i=0;i<ch;i++){
        if(ch[i]>='0'&&ch[i]<='9')
    }

    return 0;
}